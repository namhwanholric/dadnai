# HANDOFF — 달빛서고 웹소설 사이트

**갱신:** 2026-08-02
**위치:** `C:\aiai\webnovel-site` (별도 git 저장소. 부모 저장소 `C:\aiai`의 `.gitignore`에서 제외됨)

사용법·회차 추가 방법·배포 방법은 [README.md](README.md)에 있습니다. 이 문서는 **작업 상태**만 적습니다.

---

## 1. 완료된 것

| 항목 | 상태 |
| --- | --- |
| 빌드 검증 | `npm run typecheck`, `npm run build` 모두 통과 (정적 페이지 21개 생성) |
| 샘플 원고 | 3작품 11회차 전부 작성 (회색 탑 5화 / 편의점 3화 / 밤의 교정자 3화 완결) |
| 데이터 정합성 | 「밤의 교정자」 "전 8화 완결" → **"전 3화 완결"** 로 수정 (`content/series.ts`, 표지 SVG 양쪽) |
| README | 작성 완료 |
| 배포 워크플로 | `.github/workflows/deploy.yml` 작성 완료 (Pages, `NEXT_PUBLIC_BASE_PATH` 자동 주입) |
| git | `git init` + 초기 커밋 완료 (원격 저장소 없음) |

### 실제로 브라우저에서 확인한 것 (375px 모바일 폭)

- 홈 / 둘러보기 / 내 서재 / 작품 상세 / 읽기 화면 — 가로 스크롤 없음 (`scrollWidth == 375`)
- 둘러보기 필터(상태·장르·태그) 동작
- 내 서재 "이어 읽기" 목록에 방문한 회차가 쌓임
- 영상: 재생 버튼을 누르기 전 iframe 0개 → 누르면 `youtube-nocookie.com` 임베드 1개 생성
- 잘못된 영상 주소는 재생기 대신 안내 문구로 대체됨 (4화에서 확인)
- 본문 속 영수증 상자(코드 블록)는 글자 크기를 24px로 키워도 상자 안에서만 가로 스크롤됨
- 읽기 화면 상단 헤더 53px / 하단 탭바 68px, 본문 아래 여백 96px → 본문이 가려지지 않음

### 이번에 같이 고친 것

- `globals.css`: 본문 코드 블록(`pre`/`code`) 스타일 추가. 영수증·계약서 같은 인쇄물을 고정폭으로 보여 주고,
  폭이 넘치면 **상자 안에서만** 가로 스크롤되게 했습니다. (없으면 모바일에서 페이지 전체가 밀림)
- `VideoSection.tsx`: 제목이 "이번 회차의 영상 · OST"로 고정돼 있어 OST가 없는 회차에서 어색했습니다.
  → "이번 회차의 영상"으로 바꾸고, 종류는 카드 배지로만 표시합니다.
- `C:\aiai\.claude\launch.json`: 개발 서버·정적 미리보기 실행 설정 추가.
  **주의** — `next dev`를 `C:\aiai`에서 띄우면 `process.cwd()` 기준인 원고 로더가 회차를 못 찾아 "총 0화"가 됩니다.
  반드시 `webnovel-site` 폴더 안에서 실행해야 합니다. (launch.json에 `cwd`로 지정해 둠)

---

## 2. 남은 일

### 1) 실제 기기/브라우저에서 스크롤 동작 확인 — **미확인**

자동화 환경에서는 브라우저 창이 실제로 그려지지 않아 스크롤이 발생하지 않았고,
그래서 **스크롤에 반응하는 기능만 확인하지 못했습니다.**

- 상단 진행률 바가 차오르는지
- 읽는 동안 상·하단 바가 자동으로 숨는지 / 위로 올리면 다시 나오는지
- 진행률이 저장돼서 다음에 들어갔을 때 읽던 자리로 돌아가는지 (내 서재의 % 표시)

```bash
cd C:/aiai/webnovel-site && npm run dev
```
`http://localhost:3000/series/gray-bell-tower/silent-bell/` 를 폰이나 브라우저 개발자 도구(375px)로 열어 확인하면 됩니다.

### 2) 내용 교체 (샘플 → 실제)

- `content/site.ts`의 작가 링크가 `jaeha@example.com`, `https://github.com/` 로 비어 있습니다.
- 모든 회차의 샘플 영상이 Big Buck Bunny(`aqz-KE-bpKQ`)입니다. 설명란에 "예시 영상"이라고 적어 두었습니다.
- `04-what-i-paid.md`의 두 번째 영상은 **일부러 깨뜨려 둔 주소**입니다 (폴백 시연용). 실제 운영 전에 지우거나 교체하세요.

### 3) GitHub 저장소 연결 + Pages 켜기

로컬 커밋만 되어 있고 원격이 없습니다. README의 "GitHub Pages에 배포하기" 절차대로 하면 됩니다.
저장소를 만들고 푸시한 뒤 **Settings → Pages → Source를 "GitHub Actions"로** 바꿔야 배포가 돕니다.

---

## 3. 알아두면 좋은 것

- **개발 서버에서 `/browse` 필터가 회색 상자로 멈춰 보입니다.** `output: 'export'` + `useSearchParams` 조합의
  개발 모드 한정 현상이고, `npm run build` 결과물에서는 정상입니다. (README에도 적어 두었습니다)
- 테마는 사이트 전역(`<html data-theme>`)에 적용됩니다. 읽기 화면에서 어둡게 바꾸고 목록으로 나와도 유지됩니다.
- 진행률은 px이 아니라 **문서 대비 비율**로 저장합니다. 글자 크기를 바꿔도 위치가 유지됩니다.
- 좋아요·읽음 기록은 서버로 가지 않습니다. 안내 문구를 `storage.ts` 주석 / 푸터 / 읽기 화면 하단 세 곳에 넣어 두었습니다.
- 설계 근거는 [PROJECT_PLAN.md](PROJECT_PLAN.md)에 있습니다.
